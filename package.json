{
  "name": "erp",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "scripts": {
    "clean": "npm run rms; npm run installs",
    "install-api": "cd ./erp-api; npm install; cd ..",
    "installs": "npm run install-api",
    "rm-api": "cd ./erp-api; npm rm erp-config; cd ..",
    "rms": "npm run rm-api",
    "start": "npm run clean; npm run start-api",
    "start-api": "node ./erp-api/app",
    "test": "echo Coming soon..."
  },
  "dependencies": {},
  "devDependencies": {
    "bower": "^1.3.12",
    "jasmine-node": "^1.14.5",
    "jshint": "^2.5.6"
  }
}
